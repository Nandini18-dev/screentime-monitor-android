package com.nandini.screentime.data;

/** Simple aggregate row: a package name and a summed value (ms, bytes, or count). */
public class PackageTotal {
    public String packageName;
    public long total;
}
